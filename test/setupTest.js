

// Este archivo se ejecuta antes de todos los tests de integración
// Puedes agregar aquí lógica global de limpieza o migración si lo necesitas

beforeAll(async () => {
    
    // Si quieres limpiar todas las tablas antes de los tests, puedes hacerlo aquí
    
});

afterAll(async () => {
  // Cierra la conexión global de Sequelize al finalizar todos los tests
});
